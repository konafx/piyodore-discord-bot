{
  "name": "piyodore-discord-bot",
  "packageManager": "yarn@3.3.1",
  "scripts": {
    "schema:format": "prisma format",
    "db:generate": "yarn pnpify prisma generate",
    "db:migrate": "yarn pnpify prisma migrate dev",
    "db:migrate:reset": "yarn pnpify prisma migrate reset",
    "format": "prettier --write \"src/**/*.ts\" \"test/**/*.ts\"",
    "start": "node ./.dist/main.ts",
    "prebuild": "rimraf ./.dist",
    "build": "yarn db:generate && yarn run prebuild && tsc -p ./",
    "dev": "yarn db:generate && ts-node src/main.ts",
    "dev:watch": "nodemon --watch src --ext ts --exec ts-node --ignore '*.test.ts' src/main.ts",
    "dev:container": "docker compose up -d",
    "dev:container:nocache": "yarn run dev:container --build"
  },
  "dependencies": {
    "@prisma/client": "4.8.1",
    "discord-api-types": "^0.37.26",
    "discord.js": "^14.7.1"
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "@yarnpkg/pnpify": "^4.0.0-rc.35",
    "eslint": "^8.31.0",
    "eslint-config-prettier": "^8.6.0",
    "nodemon": "^2.0.20",
    "prettier": "^2.8.2",
    "prisma": "4.8.1",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  }
}
